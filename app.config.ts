export default defineAppConfig({
    ui: {
        primary: 'blue',
        gray   : 'neutral',

        notifications: {
            position: 'end-0'
        },

        modal: {
            container: 'flex min-h-full items-start justify-center text-center'
        }
    }
});